services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    env_file:
      - 1panel.env
    environment:
      NODE_ENV: production
    restart: unless-stopped
    volumes:
      - ./public:/app/public